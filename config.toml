baseURL = "https://fortinetcloudcse.github.io/DemoFrontEndDocker/"
languageCode = "en-US"
defaultContentLanguage = "en"
canonifyURLs = true
relativeURLs = true

title = "Hugo web framework for Fortinet Demos"
theme = ["hugo-notice", "hugo-theme-relearn"]
metaDataFormat = "yaml"
defaultContentLanguageInSubdir= "false"
uglyurls = "true"
enableEmoji = true

[params]
  # Prefix URL to edit current page. Will display an "Edit this page" button on top right hand corner of every page. 
  # Useful to give opportunity to people to create merge request for your doc.
  # See the config.toml file from this documentation site to have an example.
  #editURL = ""
  # Author of the site, will be used in meta information
  author = "Jeff Kopko"
  # Description of the site, will be used in meta information
  description = "Hugo web framework for Fortinet Demos"
  # Shows a checkmark for visited pages on the menu
  showVisitedLinks = true
  # Disable search function. It will hide search bar
  disableSearch = false
  # Javascript and CSS cache are automatically busted when new version of site is generated. 
  # Set this to true to disable this behavior (some proxies don't handle well this optimization)
  disableAssetsBusting = false
  # Set this to true to disable copy-to-clipboard button for inline code.
  disableInlineCopyToClipBoard = false
  # A title for shortcuts in menu is set by default. Set this to true to disable it. 
  disableShortcutsTitle = false

  # If set to false, a Home button will appear below the search bar on the menu.
  # It is redirecting to the landing page of the current language if specified. (Default is "/")
  #disableLandingPageButton = true

  # When using mulitlingual website, disable the switch language button.
  disableLanguageSwitchingButton = true
  # Hide breadcrumbs in the header and only show the current page title
  disableBreadcrumb = false


 # If set to true, hide table of contents menu in the header of all pages
  disableToc = false
  # If set to false, load the MathJax module on every page regardless if a MathJax shortcode is present
  disableMathJax = false
  # Specifies the remote location of the MathJax js

  customMathJaxURL = "https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js"
  # Initialization parameter for MathJax, see MathJax documentation
  mathJaxInitialize = "{}"
  # If set to false, load the Mermaid module on every page regardless if a Mermaid shortcode or Mermaid codefence is present
  disableMermaid = false
  # Specifies the remote location of the Mermaid js
  customMermaidURL = "https://unpkg.com/mermaid/dist/mermaid.min.js"
  # Initialization parameter for Mermaid, see Mermaid documentation
  mermaidInitialize = "{ \"theme\": \"default\" }"
  # If set to false, load the Swagger module on every page regardless if a Swagger shortcode is present
  disableSwagger = false
  # Specifies the remote location of the RapiDoc js
  customSwaggerURL = "https://unpkg.com/rapidoc/dist/rapidoc-min.js"
  # Initialization parameter for Swagger, see RapiDoc documentation
  swaggerInitialize = "{ \"theme\": \"light\" }"
  # Hide Next and Previous page buttons normally displayed full height beside content
  disableNextPrev = true
  # Order sections in menu by "weight" or "title". Default to "weight";
  # this can be overridden in the pages frontmatter
  ordersectionsby = "weight"

  # Change default color scheme with a variant one. Eg. can be "auto", "red", "blue", "green" or an array like [ "blue", "green" ].
  #themeVariant = "auto"
  #themeVariant = "red"
  themeVariant = "Fortinet"
  #themeVariant = "mine"
  #themeVariant = "relearn-dark"
  # Change the title separator. Default to "::".
  titleSeparator = "-"
  # If set to true, the menu in the sidebar will be displayed in a collapsible tree view. Although the functionality works with old browsers (IE11), the display of the expander icons is limited to modern browsers
  collapsibleMenu = false
  # If a single page can contain content in multiple languages, add those here
  additionalContentLanguage = [ "en" ]
  # If set to true, no index.html will be appended to prettyURLs; this will cause pages not
  # to be servable from the file system
  disableExplicitIndexURLs = false
  # For external links you can define how they are opened in your browser; this setting will only be applied to the content area but not the shortcut menu
  externalLinkTarget = "_blank"


  #landingPageURL = "/DemoFrontEndDocker"
  #landingPageName = "<i class='fas fa-home'></i> Home"



[outputs]
home = [ "HTML", "RSS", "JSON", "SEARCH", "SEARCHPAGE"]

[[menu.shortcuts]] 
name = "<i class='fas fa-graduation-cap'></i> AWS Sample Workshop"
url = "https://aws-samples.github.io/aws-modernization-workshop-sample/"
weight = 10

[[menu.shortcuts]]
name = "<i class='fas fa-tools'></i> Fortinet Standard Workshop Web format - MVP1"
url = "https://fortinetsecdevops.github.io/technical-recipe-azure-sdwan/"
weight = 11

[[menu.shortcuts]]
name = "<i class='fas fa-tools'></i>Fortinet Standard Workshop Git Repo - MVP1"
url = "https://github.com/FortinetSecDevOps/technical-recipe-azure-sdwan"
weight = 21

[[menu.shortcuts]]
name = "<i class='fas fa-tools'></i> Hugo learn theme repo"
url = "https://fortinetsecdevops.github.io/technical-recipe-azure-sdwan/"
weight = 30

[[menu.shortcuts]]
name = "<i class='fas fa-tools'></i> Hugo Learn theme - Guide"
url = "https://learn.netlify.app/en/basics/"
weight = 35


[[menu.shortcuts]]
name = "<i class='fas fa-tools'></i> Hugo Notice Submodule "
url = "https://github.com/martignoni/hugo-notice"
weight = 40

[markup]
  defaultMarkdownHandler = "goldmark"
  [markup.goldmark]
    hrefTargetBlank = true

[Langauges]
  #landingPageURL = "/DemoFrontEndDocker/"
  landingPageName = "<i class='fas fa-home'></i> Home"